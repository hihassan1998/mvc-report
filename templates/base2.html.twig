<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8" name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>
			{% block title %}
				Welcome!
			{% endblock %}
		</title>
		<link rel="icon" href="{{ asset('img/hh_logo_b.png') }}"/>
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body class="proj-body">
		<header class=" proj-header" style="background-image: url({{ asset('img/malen.png') }})">
			<span class="site-title">Hassans Symfony Site</span>
		</header>
		<nav>
			 <ul class="main-nav">
				<li>
					<a href="{{ path('home') }}">Hem</a>
				</li>
				<li>
					<a href="{{ path('report') }}">Report</a>
				</li>
				<li class="dropdown">
					Project ▾
					<ul class="dropdown-content">
						<li>
							<a href="{{ path('app_proj') }}">Start</a>
						</li>
						<li>
							<a href="{{ path('app_proj_about') }}">About</a>
						</li>
						<li>
							<a href="{{ path('about') }}">Indicators</a>
						</li>
						<!-- Add more project-related links here -->
					</ul>
				</li>
			</ul>
		</nav>

		{% block body %}{% endblock %}
		<footer class="proj-footer">Denna websida är del av kursarbetet som görs under kursen MVC på Blekinge Tekniska Högskola.</footer>
	</body>
</html>
